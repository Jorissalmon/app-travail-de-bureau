# Copy to android/keystore.properties (git-ignored) and fill in.
# Create the keystore once — see README "Signer et publier l'APK".
storeFile=/absolute/path/to/releve-release.jks
storePassword=CHANGE_ME
keyAlias=releve
keyPassword=CHANGE_ME
